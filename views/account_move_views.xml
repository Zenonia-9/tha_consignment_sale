<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_consignment_settlement" model="ir.ui.view">
        <field name="name">account.move.form.consignment.settlement</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_consignment_settlement"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-files-o"
                        invisible="not tha_consignment_settlement_id">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Consignment Settlement</span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
