<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="consignment_order_layout_a4">
        <t t-if="not o" t-set="o" t-value="doc"/>
        <t t-if="not company">
            <t t-if="company_id">
                <t t-set="company" t-value="company_id"/>
            </t>
            <t t-elif="o and 'company_id' in o and o.company_id.sudo()">
                <t t-set="company" t-value="o.company_id.sudo()"/>
            </t>
            <t t-else="">
                <t t-set="company" t-value="res_company"/>
            </t>
        </t>
        <t t-set="header_attachment" t-value="o.env.ref('tha_consignment_sale.attachment_ytk_consignment_header', raise_if_not_found=False)"/>

        <div t-attf-class="header o_company_#{company.id}_layout">
            <div class="ytk-consignment-header">
                <img t-if="header_attachment and header_attachment.datas"
                     t-att-src="image_data_uri(header_attachment.datas)"
                     style="width: 100%; height: auto; display: block;"
                     alt="YTK Header"/>
            </div>
        </div>

        <div t-attf-class="article o_report_layout_standard o_table_standard o_company_#{company.id}_layout"
             t-att-data-oe-model="o and o._name"
             t-att-data-oe-id="o and o.id"
             t-att-data-oe-lang="o and o.env.context.get('lang')">
            <t t-out="0"/>
        </div>

        <div t-attf-class="footer o_company_#{company.id}_layout">
            <div class="o_footer_content"/>
        </div>
    </template>

    <template id="consignment_order_layout_a5">
        <t t-call="tha_consignment_sale.consignment_order_layout_a4">
            <t t-out="0"/>
        </t>
    </template>
</odoo>
